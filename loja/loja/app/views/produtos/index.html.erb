<html>
    <body>
        <table>
            <thead>
                <td>Nome</td>
                <td>Descrição</td>
                <td>Preço</td>
                <td>Quantidade</td>
            </thead>
            <% todos = Produto.all %>
            <% todos.each do |produto| %>
            <tr>
                <td><%= produto.nome %></td>
                <td><%= produto.descricao %></td>
                <td><%= produto.preco %></td>
                <td><%= produto.quantidade %></td>
            </tr>
            <% end %>
        </table>
    </body>
</html>